# 2026-01-26 - Апдейт кейбиндов

- Добавлено копирование в буфер правой кнопкой мыши.
- Изменены клавиши разделения панели, теперь более логично для мнемонического запоминания:
  - Горизонтальное разделение: `Leader + -` (дефис/тире = горизонтальная линия)
  - Вертикальное разделение: `Leader + \` (на клавише так же символ |).
- Дополнил и переписал комментарии к коду в `keybindings.lua`.
- Переписал `KEYBINDINGS.md` для более удобного чтения и понимания.

# 2025-12-14 - Docs cleanup (unreleased)

- Перенёс описание клавиш из README в `KEYBINDINGS.md`, оставил ссылку на полный список.
- Проверил актуальность `KEYBINDINGS.md` по конфигу `keybindings.lua`: все сочетания отражены.

# Changelog

## 2025-12-14 - d450321 "Апдейт кейбиндов, улучшение UX"

- Добавлен правый статус-бар с workspace, hostname, временем и укороченным `cwd`; формат табов теперь `№: заголовок` с обрезкой по ширине.
- Кейбинды переработаны: тайм-аут Leader 1000 мс, навигация по панелям через `Ctrl+h/j/k/l`, ресайз через `Alt+h/j/k/l` или стрелки, быстрый выбор/обмен панелей (`g`, `{`), и модальные таблицы `resize_mode`/`pane_mode`.
- Добавлены горячие клавиши QuickSelect (`Leader + Space`), Command Palette (`Leader + ⇧ + P`) и Launcher (`Leader + ⇧ + L`); `Cmd+w` закрывает панель, `Cmd+⇧+w` - таб.
- В список fallback-шрифтов добавлен `Lilex Nerd Font`.
